import { IfUserDirective } from './if-user-directive.directive';

describe('IfUserDirective', () => {
  it('should create an instance', () => {
    const directive = new IfUserDirective();
    expect(directive).toBeTruthy();
  });
});
