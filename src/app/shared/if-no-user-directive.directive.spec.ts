import { IfNoUserDirective } from './if-no-user-directive.directive';

describe('IfNoUserDirective', () => {
  it('should create an instance', () => {
    const directive = new IfNoUserDirective();
    expect(directive).toBeTruthy();
  });
});
